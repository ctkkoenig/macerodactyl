import Foundation

/// Version and identity constants shared across the app, daemon, and panel.
/// Keep `version` in step with the Xcode target's MARKETING_VERSION.
public enum AppInfo {
    public static let version = "0.1.0"
    public static let name = "Macerodactyl"
}
