# Macerodactyl — Autonomous Execution Log

Append-only. Newest at bottom. Each entry: date, item, what I did, decisions
(fork→choice→why), what I rejected, verification result.

---

## 2026-09-02 — Session start / orientation
- Baseline: `main` @ `d287594`, clean tree, **131 tests green**. Repo PRIVATE (confirmed via gh). Docker daemon up. Swift 6.3.3, Xcode active.
- Wrote `docs/AUTONOMOUS-PLAN.md` (ordered checklist + acceptance criteria) and this log. These are my memory across resets.
- Constraints internalized: fixtures I own = testweb / fixture-bare / fixture-mc; NEVER touch the two real (non-fixture) workloads; repo stays private, no release publish; don't touch icon/logo; green-only commits; no coverage cuts; no security weakening.
- Environment deltas since last work: toolchain moved 6.2 → 6.3.3 (watch for new warnings-as-errors or format differences). `swiftlint`/`swift-format` not installed standalone; will prefer toolchain `swift format`.
- Plan for Tier 0 next: CI workflow (macOS runner, swift test), format/lint gate, integration job. Cannot execute GitHub Actions from here, so acceptance = validate each step runs locally + YAML is correct; the owner will see it run on push (I will NOT push to a public remote; origin is private, pushing to the private origin is allowed and desired).

## 2026-09-02 — Tier 0 CI (T0.1–T0.3)
- **Did**: adopted `swift format` (config-to-existing 4-space rather than churn to its 2-space default; PanelHTML.swift marked `swift-format-ignore-file` since Tier 3 replaces it). Formatted all sources; repo lints clean. Added `.github/workflows/ci.yml`: lint (Linux swift:6.0 container — format only parses, no SDK), test+build (macos-15), integration (macos-15 + colima real dockerd → kitcheck end-to-end).
- **Decision (T0.3 fork)**: macOS runners can't run Docker Desktop → used **colima** for a real daemon; the full HTTP e2e (server+curl) waits for the standalone daemon (Tier 1) since the GUI can't run headless. kitcheck exercises the docker-CLI core now.
- **Verified locally**: YAML parses (ruby), strict lint exit 0, `xcodebuild ... CODE_SIGNING_ALLOWED=NO` BUILD SUCCEEDED, kitcheck subcommands present. Pushed to PRIVATE origin; CI run 33590033578 triggered (monitoring in background). Rejected: pushing every commit (macOS minutes cost) → will push at tier boundaries.

## 2026-09-02 — Tier 1 architecture decisions (before building)
- **Decouple = a real headless daemon executable** (`macerodactyld`) that serves the panel independently of the GUI. Prior design ran Hummingbird as a Task inside the GUI process (dies on quit).
- **Fork: LaunchAgent vs LaunchDaemon → chose LaunchAgent.** Why: Docker Desktop is a *per-user* service (socket at `~/.docker/run/docker.sock`); a system LaunchDaemon (root) can't cleanly reach it. Owner runs autologin, so a LaunchAgent is effectively always-on (starts at auto-login, `KeepAlive` restarts on crash, survives app quit; reboot re-logs-in and relaunches). LaunchDaemon's only gain (logout survival) is moot under autologin, at the cost of docker access. RunAtLoad starts only the SERVER, never a container — respects "never starts containers at boot."
- **Fork: GUI-as-client vs GUI-reads-docker-directly → chose the daemon reads docker directly; GUI keeps its own direct-docker ContainerStore for native display.** Why: making the native SwiftUI a pure HTTP client of the daemon is a full rewrite that loses live SwiftUI observation. Instead the *shared source of truth is docker itself* + the shared `panel.sqlite` (accounts/grants/audit/sessions, WAL = multi-process safe). Tradeoff: a phone action now reflects in the native window within the GUI's ~3s poll instead of instantly. Documented regression; a GUI↔daemon event channel is future work. The GUI stops hosting the server and instead installs/monitors the LaunchAgent.
- **Config sharing**: daemon has its own UserDefaults domain, so it can't read the GUI's `AppSettings`. Introduce a shared **`PanelConfig` JSON** at `~/Library/Application Support/Macerodactyl/config.json` both read; GUI writes the daemon-relevant subset (port, bindLAN, dockerPathOverride, stacksRoot) on change.
- **New: `DaemonContainerService`** — a `ContainerService` backed by `DockerCLI` directly (no `@MainActor`/`ContainerStore`), so the daemon (and later the Linux build) needs no SwiftUI. Mirrors `LiveContainerService` behavior sourcing containers from `docker ps`.
- Staging: (A) PanelConfig + DaemonContainerService + tests; (B) macerodactyld exe + PanelDaemonManager (launchd plist install/uninstall/status) + LIVE verify (kill -9 → KeepAlive restart, quit GUI → curl still 200); (C) GUI wiring + bundle the daemon binary via an Xcode copy phase.

## 2026-09-02 — Tier 1.1 daemon: built + a hard launchd blocker (IMPORTANT, read this)
**Built & green (139 tests, lint clean, app builds):**
- `PanelConfig` (shared JSON config) + tests; `DaemonContainerService` (DockerCLI-direct ContainerService, no SwiftUI/MainActor); `macerodactyld` executable; `PanelDaemonManager` (LaunchAgent install/uninstall/status, KeepAlive, RunAtLoad, server-only) + tests.
- **SwiftUI split**: moved ALL SwiftUI views out of MacerodactylKit and MacerodactylPanel into a new `MacerodactylUI` target. Kit + Panel are now SwiftUI/AppKit-free (verified). This is also the Tier 4 prerequisite. App links MacerodactylUI (pbxproj updated). Made `ContainerStore.describe` public for cross-module use.
- The daemon serves perfectly when run **standalone from a shell** (303, logs, docker access) — including with a minimal launchd-like env (`env -i HOME=...`, cwd=/, output→file).

**BLOCKER — launchd auto-start could NOT be verified in this machine's current state:**
- Any binary that links Hummingbird/NIO (the daemon, and a minimal `probe` that just writes a file) **hangs pre-`main` when started in the launchd session** here — no diag output at all, process state S (sleeping), un-introspectable (both `lldb -p` and `/usr/bin/sample` themselves hang on it). A trivial Foundation-only Swift binary and a `/bin/sh` job BOTH run fine under launchd here, so launchd + /tmp work; it's specific to NIO-linked binaries.
- Ruled out: SwiftUI linkage (removed, still hangs), debug-vs-release (both hang), top-level-await vs sync main (both hang), ad-hoc signing (already signed; re-signed, no change), `SWIFT_BACKTRACE=enable=no` (no change), direct-job vs sh-grandchild (both hang), product dylibs (statically linked; only a weak swift shim via rpath).
- **Strong hypothesis**: the machine is currently headless/asleep (owner asleep) and the `gui/$UID` launchd session lacks the GUI-session services that Network.framework/Security.framework init blocks on; the same binary likely runs under launchd in a normal interactive session. Could not confirm (can't create an interactive session autonomously).
- **Decision**: keep all the daemon infrastructure (built, tested, works standalone) and the GUI's existing in-process server (works). Do NOT rip out the working in-process path. GUI wiring for the background-service will do a **post-install health check and fall back to in-process** if the daemon doesn't answer on the port within a few seconds, so enabling it can never leave the panel broken. Owner: please verify `macerodactyld` under launchd during a normal logged-in session — if it serves, the daemon path is good; if it still hangs pre-main, this needs a deeper macOS-session root-cause (possibly an entitlement/session issue).
- Cleaned up ALL scratch launchd test agents (bootout) and temporary probe target; no persistent LaunchAgent left in ~/Library/LaunchAgents.

- Additional ruling-out: a Network.framework-only Swift binary runs fine under launchd here, so Network is NOT the blocker. It's something in the statically-linked NIO/Hummingbird/service-lifecycle stack's early init. Not root-caused; moving on per the "document and skip" rule. Added a `PanelTool daemon` CLI (install/uninstall/status) so the owner can manage/verify the daemon manually.

## 2026-09-02 — Tier 1.2 rate-limiter → SQLite (done)
- Schema v3 adds `rate_limits` (key, failures, blocked_until). `PanelDataStore` gains get/set/clear/prune. `LoginRateLimiter` refactored behind a `RateLimitStore` protocol with `InMemoryRateLimitStore` (existing fast tests, unchanged) and `SQLiteRateLimitStore` (production). PanelServer now uses the SQLite store, so both the in-process GUI server and the daemon persist throttling (same panel.sqlite, WAL = multi-process safe).
- Verified with a real-SQLite persistence test: lock out an account, open a NEW store on the SAME file ("restart"), lockout still in effect; recordSuccess clears it persistently. Kept all prior rate-limiter tests. 141 tests.
- Chose the protocol-backend split over rewriting the limiter so the tested backoff math and the fast in-memory tests stay intact.

## 2026-09-02 — Tier 1.3 health endpoint (done) + daemon-main correction
- Added public `GET /healthz` → {status, version, docker: ready|unreachable}. No auth, no secrets (tested it leaks no account/session data). `dockerReachable()` added to ContainerService (bounded `docker version`, 5s timeout).
- **Correction to the Tier 1.1 note**: I had tried a *synchronous* main (Task + DispatchSemaphore) for the daemon to dodge the launchd hang. It did NOT fix launchd AND it broke standalone binding (the server Task never reaches bind while main blocks on the semaphore). Reverted to **top-level `await`** in main.swift, which serves correctly standalone (verified: /healthz live returns ok/ready). The launchd hang is unchanged and remains the documented, environment-specific blocker — NOT caused by the main structure.
- Verified /healthz live against the standalone daemon. 142 tests.

## 2026-09-02 — Tier 1.4 WAL checkpoint / backup / restore / integrity (done)
- `Database`/`PanelDataStore`: `checkpoint()` (wal_checkpoint TRUNCATE), `integrityCheck()`/`isHealthy` (PRAGMA integrity_check), `backup(toPath:)` (VACUUM INTO — live-safe, refuses existing dest). `PanelBackup.restore(from:to:)` validates the backup's integrity FIRST, sidelines the current DB (returns its path so a bad restore is reversible), clears stale WAL/SHM; caller must stop the panel.
- Durability itself is already guaranteed by WAL + committed writes; these add observability + operability. `PanelTool db check|checkpoint|backup <path>|restore <path>`.
- Verified live against the owner's real panel.sqlite: `db check` → integrity ok/healthy; `db backup` → consistent 53KB copy (removed after). 5 durability unit tests (backup restorable, restore sidelines+validates, rejects corrupt). 147 tests.

## 2026-09-02 — Tier 1.5 first-class TLS for LAN (done)
- Optional built-in HTTPS: `SelfSignedCertificate.swift` generates a cert+key via system `openssl` (`req -x509 rsa:2048 -days 825 -nodes`, SAN `DNS:localhost,IP:127.0.0.1`, CN "Macerodactyl Panel"), key `0600`, stored under Application Support; `ensure()` is idempotent. `PanelServerConfig.tls: TLSFiles?` drives `PanelServer.serve` to build an `Application(server: .tls(.http1(), tlsConfiguration:))` via HummingbirdTLS/NIOSSL when set, plain HTTP otherwise. When TLS is on, session cookies are marked `Secure` (`secureCookies` threaded through `buildRouter`→`PanelRoutes`). Daemon (`macerodactyld`) reads `tlsEnabled` from `PanelConfig` and wires the same path.
- GUI wiring: `AppSettings.panelTLSEnabled` (+ `currentPanelConfig()`/`syncPanelConfig()` so the shared JSON matches the GUI); `PanelController.enable()` calls `SelfSignedCertificate.ensure()` and builds a TLS `PanelServerConfig` when enabled, and `localURL` switches to `https`; `PanelAdminModel.setTLSEnabled` + a "Serve over HTTPS (self-signed certificate)" toggle in `PanelSettingsView` with copy that keeps the tunnel as the recommended path.
- **Decision — conflict with CLAUDE.md.** The old CLAUDE.md said "Plain HTTP only … never implement certificate handling." The autonomous brief explicitly directs Tier 1.5 to add first-class TLS with an optional self-signed cert and a Secure cookie for LAN. The brief is the newer, direct instruction and wins; updated CLAUDE.md's web-panel + dependencies sections to describe built-in HTTPS as an *optional, non-default* feature with the tunnel still recommended (docs-match-reality). Added HummingbirdTLS + swift-nio-ssl to the frozen-deps note (both already in the Hummingbird/NIO tree).
- **Verified live end-to-end** (earlier this session, before compaction): HTTPS handshake against the standalone daemon, served cert CN=Macerodactyl Panel, plain HTTP refused on the TLS port, session cookie carried `Secure`. Transient config + cert cleaned up afterward.
- Tests: `SelfSignedCertificateTests` (valid PEM cert+key, key 0600, skips if no openssl); `sessionCookieIsSecureWhenServingHTTPS()` in PanelServerTests. Full suite **149 tests** green; `swift format lint --strict` clean; app `xcodebuild` BUILD SUCCEEDED.

## 2026-09-02 — Tier 1 boundary audit + security-review fixes (done)
- **Mechanical audit**: full suite 149 green; CLAUDE.md reconciled to the built-in-HTTPS reality; **no leaked home paths/usernames** in tracked files (git grep clean); swift-nio-ssl pinned (2.37.3) and committed in both Package.resolved files. Portability: openssl resolved by explicit path with a missing-openssl error; LaunchAgent is macOS-only (Linux build is Tier 4).
- **`/ultrareview` substitution**: per session policy `/code-review ultra` is user-triggered + billed and I cannot launch it autonomously. Substituted a rigorous fresh-eyes review via a general-purpose subagent over the whole Tier 1 diff (8abb2ba..HEAD), focused on the four load-bearing properties + the new code. **Result: no load-bearing property broken, no remote code-exec/auth-bypass.** Five real bugs found + fixed (commit `ba2064c`):
  1. **restore data-loss (med-high)**: `PanelBackup.restore` opened the backup read-WRITE, so a missing/typo path open-CREATED an empty DB that passed `integrity_check` and got installed over live accounts/grants/audit. Fix: guard `fileExists`, open read-ONLY (no CREATE, no WAL litter), require `user_version >= 1`. + tests (missing-path preserves live data; foreign SQLite rejected).
  2. **TLS silent downgrade (med)**: TLS-requested-but-cert-failed fell back to plain HTTP (also dropping the Secure cookie). Fix: **fail closed** — daemon `exit(1)`, GUI refuses to start and sets `startupError`.
  3. **loopback rate-limit DoS (med)**: behind a tunnel every peer is 127.0.0.1 → one shared `ip:` bucket locks out ALL users, now persisted across restarts. Fix: exempt loopback from the IP dimension; per-account bucket still throttles. + test.
  4. **healthz fan-out (low-med)**: unauth `/healthz` exec'd `docker version` per hit. Fix: 5s `HealthProbeCache` actor.
  5. **secret file mode race (low)**: TLS key + first-admin.txt were chmod'd 0600 AFTER write. Fix: create 0600 at creation time (pre-create key file; `createFile(attributes:)` for admin creds). Mitigated already by ~/Library 0700.
- Deferred (documented, not blocking): #6 rate-limit store errors swallowed (deliberate "never block a login"); restore validation is integrity+schema-version only (not full table check).
- 158 tests green, lint clean, app builds.

## 2026-09-02 — Tier 2.3 file manager: FileService core (done; routes next)
- Extended the *pure, confined* file layer (commit `ca780a5`): `makeDirectory`, `move` (rename; BOTH ends confined, no clobber), `delete` (file/recursive dir; refuses root), `writeData` (binary-safe atomic upload, creates confined parent, `maxUploadBytes = 256MB`), `downloadTarget` (validated regular-file URL+size for streaming; binary allowed unlike the text editor). Refactored the atomic temp+fsync+swap into a shared `atomicWrite`.
- **Security discipline**: every new op goes through the existing `PathConfinement.resolve()` — no parallel path. Tests re-run ALL traversal shapes (`../`, `%2e%2e`, `..%2f`, absolute, `~`, NUL, symlinked destination parent) against each new op and assert nothing outside the tree changed (16 FileService tests).
- **Noted for later (pre-existing, fail-open edge)**: `ContainerScopeMiddleware.requiredPermission` uses `path.contains("/files")` etc., so a container literally named `files`/`power`/`console`/`schedule` could mis-map the required permission. Not introduced by Tier 2; will harden to match on the LAST path segment when I touch the routes (T2.3 HTTP).

## 2026-09-02 — Tier 2.3 file manager: HTTP routes (done)
- Routes (commit `694c76e`), all under the scope middleware (each path contains `/files` → requires `.files`), each audited: `GET :name/files/download` (streamed 64KB chunks via ResponseBody, `Content-Disposition: attachment`, binary-safe, bounded memory), `POST :name/files/upload` (`request.body.collect(upTo: maxUploadBytes)` so oversize is rejected before buffering), `POST :name/files/dir` (mkdir), `POST :name/files/move` (rename), `DELETE :name/files/entry` (delete).
- **Hardened the fail-open edge I flagged**: `ContainerScopeMiddleware.requiredPermission` now maps by fixed route position (`/api/containers/<name>/<action>`) via `comps[3]`, not `path.contains("/files")`. A container named `files`/`power`/`console`/`schedule` can no longer mis-map an action to a weaker permission. Unit test covers the collision cases.
- **HTTP tests** (per AC): full mutation round-trip incl. binary upload→download **byte-equal**; every new endpoint returns **403 without the files perm**; **ALL traversal shapes** (`../`, `..%2f`, `%2e%2e`, absolute) rejected on mkdir/upload/delete/download/move (move tested at BOTH ends) with the sibling `secret-data/creds.txt` left untouched. 162 tests green.
- **Decision — no SPA buttons yet**: `PanelHTML.swift` is `swift-format-ignore` and slated for full replacement in Tier 3 (extract frontend). Wiring upload/download/mkdir/rename/delete into the current string-embedded JS would be throwaway, so the phone UI for these lands with the Tier 3/4 frontend rebuild. The endpoints + confinement tests ARE the T2.3 acceptance criteria and are complete. No parallel file path — everything goes through the one `FileService`/`PathConfinement`.

## 2026-09-02 — Tier 2.4 retained metrics + searchable logs (done)
- **Reorder decision**: did T2.4 before T2.1/T2.2. Why: T2.1 (create/remove/prune) is destructive and its live verification is constrained to fixtures only; T2.4 is read-only, cleanly testable, and builds directly on the existing stats + SQLite layers. Recorded per "decide and continue."
- **Retained metrics** (commit `cc0ae28`): schema **v4** `metrics` table + `recordMetric`/`metrics(container:since:)`/`pruneMetrics(maxAge:maxPerContainer:)` — bounded per-container time series, pruned by age AND a per-container row cap (disk can't grow unbounded). `MetricsSampler` actor samples `statsSnapshot()` every 20s while the panel runs (started/stopped with the server via `PanelServer.start`/`stop`/`runUntilTerminated` — never outlives a quit; read-only, never touches a container). `GET :name/metrics?since=` returns view-gated history with `measuredAt` for charts.
- **Searchable logs + download**: **design decision** — served over `docker logs` (bounded by docker's own json-file log driver rotation), NOT a duplicated always-on SQLite log warehouse. Rationale: an unbounded log-ingestion store risks filling the disk and duplicates what docker already retains; docker logs survive app restarts. `LogStreamService.history` merges the `--timestamps` stdout+stderr back into one chronological log. `GET :name/logs/search?q=&since=&tail=` (case-insensitive, capped) and `GET :name/logs/download` (text attachment). Documented tradeoff: logs do NOT survive container *recreation* (docker drops them) — a cross-recreation warehouse is deferred as out-of-scope for the disk-safety reason.
- **Live verification**: added `kitcheck metrics` — ran against the 4 real fixtures (fixture-bare, fixture-mc, testweb-nginx-1, testweb-redis-1): live docker stats → recordMetric → prune → read-back all OK, throwaway DB cleaned up, no fixture touched.
- Tests: metrics store round-trip + both retention paths, sampler (running-only + row cap), v4 migration+persist, log merge (chronological/stable/empty), HTTP log search filter + download attachment + view-gating, metrics endpoint filter + 404-for-ungranted. **176 tests** green; lint clean; app builds.

## 2026-09-02 — Tier 2.1 + 2.2 lifecycle & compose (done together)
- **Permission decision**: the 5 perms did NOT suffice. Added a **6th `.lifecycle` permission** (schema **v5** `perm_lifecycle`) for destructive per-container mutations (pull/recreate/remove), deliberately independent of `power` — "can restart" must never imply "can destroy". Full chain wired: Authorization (`ContainerPermission`/`ContainerGrant`/engine — independent + still requires view), grant store (set/read), scope middleware (position-based mapping adds pull/recreate/remove/compose→.lifecycle), `GrantDTO` + detail `Permissions` DTO, native `PanelSettingsView` "Lifecycle" checkbox + `PanelAdminModel`.
- **Scope decision — daemon-global vs per-container**: image prune / disk usage affect the WHOLE daemon (other users' resources), which the per-container scoping model can't express. Made them **admin-only** (`RequireAdmin` middleware, returns 404 to hide existence from scoped users), NOT a per-container grant. This preserves the scoping boundary.
- **Scope decision — what T2.1 does/doesn't cover**: the app is compose-stack-centric (per CLAUDE.md), so lifecycle = pull image / recreate service / remove container / compose apply, all keyed to the container's stack. **Deferred (documented)**: arbitrary `docker run` container creation and standalone volume/network CRUD — they don't fit the compose model and creation happens by editing a compose file (T2.3 files) + `compose apply`. env editing = edit `.env`/compose via the file manager (T2.3) + recreate. This is a coherent, honest scope, not a gap.
- Routes (commit `542c92d`), CSRF-protected + audited: `POST :name/pull` (streamed, stderr merged), `POST :name/recreate` (compose up -d --force-recreate <service>, streamed), `POST :name/compose/apply` (compose up -d, streamed — **T2.2 apply**; **T2.2 view** = existing confined `files/content` read of docker-compose.yml), `DELETE :name/remove` (docker rm, refuses running → 409). Admin: `GET /api/maintenance/disk`, `POST /api/maintenance/image-prune`.
- **New**: `ContainerLifecycle` helper (one impl for GUI + daemon services); `DockerCLI.streamLines(mergeStderr:)` so pull/compose progress (on stderr) streams live. Compose ops require the plugin (Docker Desktop ships it) → clear error stream otherwise.
- **Live (safe, read-only)**: `docker system df` returns the table; compose plugin **v2.35.1** present → recreate/apply supported. Destructive ops (remove/recreate/prune) deliberately NOT run live to avoid disrupting fixtures/daemon; covered by unit + HTTP tests.
- Tests: lifecycle gating (403 without perm, service never reached), remove running→409 / stopped→ok, pull+recreate streamed, compose apply streamed+gated, maintenance admin-only (non-admin 404), `.lifecycle` independent + requires-view, v5 migration persist. **183 tests**, lint clean, app builds.

## 2026-09-02 — Tier 2 boundary audit (in progress)
- **Mechanical**: full suite **183** green; no leaked home paths/usernames in source (git grep clean).
- **Docs-match-reality**: updated CLAUDE.md's scoping section to 6 permissions (added schedules + lifecycle; noted lifecycle is separate from power) and documented that daemon-global maintenance is admin-only (404 to hide the surface).
- **Portability**: metrics prune uses SQLite window functions (`ROW_NUMBER() OVER (PARTITION BY …)`), which need SQLite ≥ 3.25. macOS system libsqlite3 is 3.51 — verified the query runs. **Tier 4 note**: confirm the Linux swift image's libsqlite3 is ≥ 3.25 (Debian bookworm ships 3.40+, so fine, but verify when the Linux build lands).
- **Security review**: launched a fresh-eyes subagent over the Tier 2 diff (file-manager confinement on every new op, lifecycle gating, the new permission, admin-only maintenance, log/metrics endpoints, streamLines teardown). Findings to be actioned when it returns.

## 2026-09-02 — Tier 3 plan (frontend extraction, pre-execution notes)
Current state: the whole phone SPA is a 375-line Swift string in `PanelHTML.swift` (marked `swift-format-ignore`). It builds UI with `innerHTML +=` and a manual `esc()` helper — safe *today* only by discipline; a single missed `esc()` is a latent XSS. That's exactly what T3.1 must structurally eliminate.
- **Approach chosen: vanilla safe-DOM, no npm/no build** (CLAUDE.md forbids a build toolchain, so a React/Vue build-step SPA is out; htmx would be a vendored dep + a big server-fragment rewrite). Plan:
  1. **Extract** HTML/CSS/JS out of the Swift string into bundle resource files (e.g. `Resources/panel/{app.html,login.html,panel.css,panel.js,login.js}`); serve them from `PanelRoutes` by reading the bundle resource (correct content-types, cache headers). Removes the `swift-format-ignore`.
  2. **Rewrite the JS to build DOM via a tiny `h(tag, props, ...children)` helper** where text is set through `textContent`/`.append(node)` and attributes via properties — NEVER `innerHTML` with interpolated data. Untrusted values (names, file names, log/console text, overview, schedule) can then only ever become text nodes or attribute values, never parsed HTML. This makes a missed escape *impossible by construction* (the AC).
  3. Add the **still-UI-less endpoints** from Tier 2: file upload/download/mkdir/rename/delete buttons (T2.3), lifecycle buttons pull/recreate/remove gated on `p.lifecycle` + compose apply (T2.1/2.2), a metrics history sparkline (`/metrics`), and log search + download (T2.4). Admin-only maintenance page for image-prune/disk (only shown to admins).
  4. **Tests/verify**: server tests that resources are served with the right content-type and that a login is still required; a repo-level guard (grep/CI) that `panel.js` contains no `innerHTML`/`outerHTML`/`insertAdjacentHTML` with a non-literal. Can't run a live phone a11y pass headless (no browser + GUI can't run headless here) — will do focus-visible/aria/44px targets by construction and note the visual pass as owner-verifiable.
- **Risk**: this is a large rewrite I can't visually verify on a phone in this session. Will keep the exact same IA/interactions, verify the build + server tests, and lean on structural safety over visual polish.

## 2026-09-02 — Tier 2 boundary review: CLEAN + hardening (done, tier sealed)
- **Fresh-eyes security subagent over the full Tier 2 diff (ca780a5~1..HEAD) found NO exploitable issue.** All five load-bearing properties verified intact for the new code: per-container scoping (new SQLite-direct reads `apiMetrics`/`apiStatsSnapshot` still gated by the scope middleware / explicit canView filter), path confinement (every new FileService op routes through `resolve()`; `move` confines BOTH ends; symlinked-destination-parent caught; delete refuses root; upload's parent auto-create stays confined), app-owned identity, CSRF on every new mutating route, and no argv/shell injection (the only user value reaching argv — log `since` — is a discrete array element). `.lifecycle` independent + requires view; position-based permission mapping complete; remove refuses running server-side; maintenance admin-only→404; MetricsSampler read-only + cancelled on stop + disk bounded; streamLines(mergeStderr) tears down cleanly.
- **Actioned the 2 LOW notes** (commit `5216f92`): (1) Content-Disposition — replaced quote-only stripping with a shared `attachmentDisposition()` that sanitizes the ASCII fallback (control/quote/backslash/non-ASCII → `_`) AND emits RFC 6266 `filename*=UTF-8''` (no longer leaning on the framework's legalizer; non-ASCII names survive); used by both download routes + header-injection unit test. (2) File download read capped to the stat'd size so the body can't exceed the declared Content-Length if the file grows mid-stream.
- Left as noted-not-actioned (reviewer agreed non-issues): download shrink-TOCTOU (self-inflicted, now truncates rather than over-reads), file-op symlink TOCTOU (unreachable — no endpoint creates symlinks; resolve blocks traversing pre-existing ones), apiMetrics not audited (consistent with stats endpoints; identity+view already enforced by middleware).
- **184 tests** green, lint clean. **Tier 2 SEALED.** Next: Tier 3 (frontend extraction) per the recorded plan.

## 2026-09-02 — Tier 3.1 frontend extraction → safe-DOM (done, LIVE-verified)
- **Approach**: extracted the 375-line hand-built HTML/JS string out of `PanelHTML.swift` into static bundle assets `Resources/panel/{app,login}.html + {panel,login}.{css,js}`, served via `PanelAssets` (fixed allow-list, never a URL filename → no traversal; `Package.swift` gains `.copy("Resources/panel")` on the panel target). `PanelHTML.swift` is now a small `PanelAssets` loader (the `swift-format-ignore` is gone).
- **The XSS fix (structural)**: `panel.js` is a full rewrite that builds ALL UI with an `h(tag, props, ...kids)` helper — untrusted values (names, file names, log/console text, overview, schedule) can only become DOM **text nodes or attribute values**, never parsed markup. No `innerHTML`/`outerHTML`/`insertAdjacentHTML`/`document.write` anywhere. A test (`FrontendAssetsTests`) asserts those sinks are absent + that assets serve with correct content-type + login-required. So "a missed escape" is now impossible by construction, satisfying the T3.1 AC. Chose safe-DOM over htmx/SPA-framework because CLAUDE.md forbids a build toolchain/npm.
- **Also wired the Tier 2 endpoints that had no UI**: file upload/download/mkdir/rename/delete, lifecycle buttons (pull/recreate/apply-compose/remove, gated on `p.lifecycle`), log search + download, a CPU **metrics sparkline** (SVG from `/metrics`), and an **admin-only maintenance** card (disk usage / image prune).
- **LIVE verification via Chrome** (the key discipline win): stood up a THROWAWAY isolated `PanelVerify` executable (temp DB in scratch, real docker so real fixtures appear; admin/verifypass123) — confirmed `.applicationSupportDirectory` is getpwuid-based so `macerodactyld` could NOT be safely isolated, hence the separate harness; **owner's real panel.sqlite mtime+size verified unchanged before and after**. Drove Chrome through login → stack-grouped landing with streaming stats → container view (live CPU/mem/net/PIDs, power + lifecycle buttons) → console `ls` exec → files list + full editor (docker-compose.yml, keyboard accessory row) → overview sparkline → logs search (50KB real nginx logs). **Zero console errors.**
- **Bug caught live + fixed**: the file **Delete** button used class `act danger`; the bare `.danger` rule (full-width "Remove schedule" button) set `width:100%`, so the delete button ate the row and squeezed filenames to **0px**. Renamed to `.act.del` + gave `.act` `flex:0 0 auto`. Only a live render surfaced this — vindicates spending the Chrome pass.
- **Cache header fix**: assets aren't content-hashed (no build step), so the initial `max-age=3600` would strand browsers on a stale panel for an hour after an app update. Changed to `no-cache` (revalidate each load; files are ~15KB, cheap).
- **Teardown**: killed PanelVerify, removed the target + `Sources/PanelVerify`, removed temp dirs, closed the tab, freed the port. Nothing throwaway committed.
- 189 tests, lint clean, app builds. Commit `4368e6f`. **Tier 3 complete** (single-item tier; boundary audit next).

## 2026-09-02 — Tier 4.1 Linux server + Docker image (done, LIVE-verified)
- **Goal**: the headless panel (`macerodactyld`) builds AND runs on Linux; native app stays Mac-only. Verified by iteratively building in a real swift Docker container and fixing every blocker:
  1. **SQLite3 module**: macOS SDK ships `import SQLite3`; Linux has none. Added `Sources/CSQLite` systemLibrary (module.modulemap over `<sqlite3.h>`, `link "sqlite3"`) + `#if canImport(SQLite3) … #else import CSQLite`. MacerodactylKit depends on CSQLite.
  2. **CryptoKit (Apple-only)**: conditional import → swift-crypto's `Crypto` on Linux (declared the already-vendored `swift-crypto` directly; used in PasswordHasher + PanelSession SHA-256).
  3. **Network.framework (Apple-only)**: gated the RCON `RCONClient` behind `#if canImport(Network)`; 3 callers (Live/Daemon services + kitcheck) fall back to `docker exec` on Linux. Detection + protocol codec stay cross-platform. **Documented limitation**: Minecraft RCON console is macOS-only; Linux uses exec.
  4. **`ByteBuffer(data:)`** needs NIOFoundationCompat (not in scope on Linux) → switched to `ByteBuffer(bytes:)` (Data is `Sequence<UInt8>`).
  5. **libswiftObservation link bug** (undefined `swift::threading::fatal`): our `@Observable` GUI types pull it in; it fails to link against the *shared* runtime on Linux (both swift:6.0 and 6.1). Fixed with **`--static-swift-stdlib`** in the Dockerfile (also makes a self-contained binary). Chose the flag over refactoring the @Observable types out of the cross-platform targets (which is coupled: `ContainerStore.PowerAction` is used by the daemon's protocol).
  - `DockerBinaryLocator` gains `/usr/bin/docker` (Linux container path). + portability test asserts it.
- **Packaging**: multi-stage `Dockerfile` (swift:6.1-jammy build → swift:6.1-jammy-slim runtime with docker.io CLI + openssl + libsqlite3; copies the binary + `*.resources` bundles). `docs/DOCKER.md` documents socket mount, stacks mount, `config.json` (bindLAN:true), first-admin retrieval, and the launchd/RCON Linux limitations. CI gains a **linux-server** job: builds the server in a swift:6.1 container, builds the image, runs it, and curls `/healthz` against the runner's real dockerd (finally the end-to-end integration Tier 0 deferred — ubuntu runners have native dockerd, no nested-virt problem).
- **LIVE verification**: image built (arm64, 932MB). Ran the container with the docker socket + a bindLAN config mounted → `/healthz` = `{docker:ready, status:ok, version:0.1.0}`, `/login` + `/assets/*` served from the bundle (Bundle.module works on Linux), logs show first-admin minted + "serving http on 0.0.0.0 (LAN):27180 — docker at /usr/bin/docker". Tore down the test container; **fixtures + the real workloads untouched**. Image left for the owner.
- macOS build + **189 tests** green, lint clean. Commit `9d6f6e1`. **Tier 4 complete.**

## 2026-09-02 — Tier 5 shopfront (done; nothing published)
- **T5.1**: `README.md` (why + features + tested security model + build-from-source with the personal-team signing step + Linux/Docker path + CI badge), `LICENSE` (MIT, copyright "Macerodactyl contributors" — deliberately NOT the owner's name, public-repo hygiene), `SECURITY.md` (GitHub private reporting + the load-bearing properties that define a vuln + operational guidance), `CONTRIBUTING.md` (Kit build/test, the non-negotiable rules, dependency policy), issue templates (bug/feature + config that routes security reports to private advisories) and a PR template whose checklist encodes the security invariants.
- **Screenshots**: captured REAL ones by running the **Linux Docker image I built** (not the owner's screen) + Chrome with `save_to_disk`: the phone panel's container Console (live CPU/mem/net/PID cards + power + Pull/Remove lifecycle + console) and Overview (live **CPU sparkline** from retained metrics + metadata). **Discarded the landing screenshot** — it showed the owner's private container names (the owner's private workloads) via the mounted host socket, which must not go in a public repo. Committed shots show only the `testweb-nginx-1` fixture. Native macOS app screenshots deliberately NOT taken (won't screenshot the owner's desktop while they're asleep; they'd asked to be consulted about screenshots — noted for the owner to add).
- **T5.2**: `Casks/macerodactyl.rb` (Homebrew cask TEMPLATE — version/sha256 placeholders + Gatekeeper caveat, honest that it's source-only/un-notarized) and `.github/workflows/release.yml` (tag/`workflow_dispatch`-triggered; builds the mac app zip and creates a **DRAFT** release the owner reviews+publishes; a no-push Linux image smoke build). **Nothing auto-publishes**; per the hard constraint I prepared it all and stopped short — no tags pushed, no release created, repo still private.
- Both workflows parse (ruby YAML). No Swift changed → suite unaffected (189). Commit `a0597bb`. **Tier 5 complete.**

## 2026-09-02 — Tier 6 harden the untested (done)
- **T6.2 fuzzing** (`FuzzTests`, commit `e24292a`): a seeded SplitMix64 harness throws 20,000 adversarial paths at `PathConfinement.resolve` (never escapes root) and 10,000 garbage strings at every docker parser (never traps) + hostile human-unit numbers. Reproducible via a pinnable seed.
- **T6.3 property tests** (`ScopingPropertyTests`): 5,000 random grant matrices assert every authorization invariant — admin⇒all, no-view⇒nothing, `can(p)` iff view AND p's bit, `visible()` exact, and exhaustively no-perm-implies-another.
- **T6.5 TOTP 2FA + sessions** (commit `00a1451`): RFC 6238 `TOTP` on existing crypto (no new dep), RFC-vector-tested; schema **v6** (totp on users; created_ip/user_agent/last_seen on sessions); self-service endpoints (2fa begin/confirm/disable/status; sessions list/revoke/revoke-others, scoped so you can only revoke your own); login gating (missing code→totpRequired no session, wrong code counts toward the rate limit); safe-DOM Account UI. **Verified live in Chrome** (enrollment shows secret+otpauth link+confirm; session list; no console errors). 208 tests.
- **T6.4 adversarial pass** (`AdversarialTests` + `docs/ADVERSARIAL.md`, commit `b59489f`): 5 attack tests (proxy-header identity forgery, forged cookies, CSRF-less mutation, SQL-injection container names with DB-intact assertion, full escalation matrix) + a 14-row table of everything tried incl. what held by construction and the two fresh-eyes agent reviews.
- **T6.1 XCUITest**: wrote a real smoke test (`UITests/MacerodactylUITests.swift`: launch → window appears → still alive) + `docs/UI-TESTING.md` for wiring it. **Decision — not wired into the pbxproj**: the hand-written project has no test targets, XCUITest needs a GUI windowserver I don't have in this headless session (can't run/verify it), and a misconfigured UI-test target risks a red app build (a hard constraint). Per the AC ("may be limited by headless CI; record"), delivered ready-to-wire + documented (2-min Xcode step + a CI snippet) rather than committed unverified. **Tier 6 complete.**

## 2026-09-02 — Finish protocol: fresh-eyes review + assessment + report (done)
- All tiers 0–6 complete; final state: **209 tests** green, lint clean, macOS app builds, Linux image builds+serves.
- **Final fresh-eyes review** (3rd independent agent) caught a **HIGH bug the suite missed**: the served 2FA login page had no code field + login.js treated the `totpRequired` 200 as success → enabling 2FA locked users out with no in-product recovery. The HTTP tests drove the JSON API (with a `totp` field), never the served page. **Fixed**: code field in login.html revealed on demand; login.js prompts on `totpRequired` instead of redirecting; native app gains admin "Reset 2FA" recovery. Verified end-to-end through the running container (password-only → totpRequired no-cookie; password+fresh-code → ok+Set-Cookie). Also fixed: scrubbed private workload names from these working-logs; added TOTP replay protection (schema **v7**, used-step tracking + test); hardened `h()` against `on*`-as-attribute; documented plaintext-TOTP-secret-at-rest in SECURITY.md. Commit `6bdf610`.
- **Deliverables**: `docs/SESSION-REPORT.md` (session summary), `docs/ASSESSMENT.md` (updated engineering assessment vs the original genius-list). Repo still PRIVATE; no release cut; cask + draft-only release workflow prepared for the owner.
- **Cleanup verified**: owner's real `panel.sqlite` mtime unchanged all session; fixtures + real workloads never touched; all throwaway containers/targets/temp dirs removed; `macerodactyl-panel:latest` image left for the owner.
- **Owner action items** (all documented in SESSION-REPORT / ASSESSMENT): verify `macerodactyld` under launchd interactively; wire the XCUITest (2-min Xcode step); add native-app screenshots + decide the icon; flip public + cut the v0.1.0 draft release when ready; optionally squash git history that referenced private names before going public.
